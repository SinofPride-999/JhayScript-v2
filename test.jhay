initiate name = "Jhay";
initiate age = 21;
initiate list_a = [1, 2, 3];
initiate list_b = [4, 5, 6];
initiate array = [];

:: if is_numeric(age) THEN echo("true");
:: if is_string(name) THEN echo("true");
:: if is_list(array) THEN echo("true");


try {
    // Whatever that is in here
} catch(error) {

}

fuck_around {

} find_out(error) {
    
}
